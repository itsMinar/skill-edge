/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;

// npx shadcn-ui@latest add accordion avatar badge toaster calendar card carousel checkbox  dialog dropdown-menu form input label popover progress select separator sheet table tabs textarea sonner

// bunx --bun shadcn@latest add accordion

// combobox
